/* exported getLastChar */

/* find the length of the string and set to variable
subtract 1 from the length and set it as the new value for the variable
return the string at variable index */

function getLastChar(string) {
  return string[string.length - 1];
}
